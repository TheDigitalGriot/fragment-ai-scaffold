{
  "name": "fragment-ai-scaffold",
  "version": "4.5.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm -w packages/create-fragment run test",
    "build": "npm -w packages/create-fragment run build",
    "prepublish": "npm -w packages/create-fragment run build"
  }
}
